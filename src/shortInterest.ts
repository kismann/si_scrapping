export interface ShortInterest {
    symbol: string;
    shortInterest: number;
    shortInterestRatio: number;
    shortPercentFloatPercent: number;
    recordDate: number | null;
    source: string;
}
